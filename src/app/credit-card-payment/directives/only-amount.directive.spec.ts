import { OnlyAmountDirective } from './only-amount.directive';

describe('OnlyAmountDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyAmountDirective();
    expect(directive).toBeTruthy();
  });
});
