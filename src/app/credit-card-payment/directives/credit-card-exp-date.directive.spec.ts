import { CreditCardExpDateDirective } from './credit-card-exp-date.directive';

describe('CreditCardExpDateDirective', () => {
  it('should create an instance', () => {
    const directive = new CreditCardExpDateDirective();
    expect(directive).toBeTruthy();
  });
});
